[tool.ruff]
target-version = "py310"
line-length = 120
extend-exclude = [
  "corpus_builds",
  "downloaded_*",
  "protected_corpora",
  "scripts/.formosan_qc_repo",
]

[tool.ruff.lint]
select = ["B", "E4", "E7", "E9", "F", "I"]

[tool.ruff.lint.per-file-ignores]
"tests/*.py" = ["E402"]
