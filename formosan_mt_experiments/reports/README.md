# Experiment Reports

This directory is for generated metrics, prediction CSVs, and analysis reports. It is ignored by git except for this README and `.gitkeep`.

Each production evaluation writes metrics JSON, a prediction CSV, and analysis
tables for the global test set plus language, source-bucket, dialect, and length
subsets. Keep these generated outputs here rather than at repository root.
