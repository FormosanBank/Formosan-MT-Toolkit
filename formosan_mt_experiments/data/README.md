# Generated Experiment Data

This ignored directory is available for local tokenizer, base-model, and split
artifacts. Production corpora remain in their named `corpus_builds/<name>/`
directory or an external shared-data root.

Do not commit corpus rows, source inventories, paid pivot caches, model files,
or manifests from authorized nonpublic sources.
